# =============================================================================
# Argos Docker Stack — Environment Configuration
# =============================================================================
# Copy this file to docker/.env and set ALL required values before deploying.
# Variables marked :? will cause Docker Compose to abort if not set.
# =============================================================================

# OpenWebRX SDR Web Interface (REQUIRED)
OPENWEBRX_PASSWORD=               # REQUIRED. No default. Set a strong password.

# Bettercap Network Recon (REQUIRED)
BETTERCAP_USER=admin
BETTERCAP_PASSWORD=               # REQUIRED. No default. Set a strong password.

# OpenCellID API Key (optional — for cell tower lookups)
OPENCELLID_API_KEY=               # Get from https://opencellid.org/

# Stadia Maps API Key (optional — for map tiles)
STADIA_MAPS_API_KEY=              # Get from https://stadiamaps.com/

# Argos API Authentication (REQUIRED — min 32 chars)
# Generate with: openssl rand -hex 32
ARGOS_API_KEY=

# Key Rotation Schedule (NIST SP 800-53 IA-5):
# - All credentials: Every 90 days or immediately upon suspected compromise
# - Generate new key: openssl rand -hex 32
